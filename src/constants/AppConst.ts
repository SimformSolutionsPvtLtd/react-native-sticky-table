const autoScrollValue = 22;
const maxWidth = 132;
const minWidth = 104;
const minOffsetThreshold = 5;
const paddingHorizontal = 32; // 32 is the padding of the text which is 16 on each side

export default Object.freeze({
  autoScrollValue,
  maxWidth,
  minWidth,
  minOffsetThreshold,
  paddingHorizontal,
});
